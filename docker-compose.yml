services:
  app:
    image: ghcr.io/mrohadi/portofolio:latest
    restart: unless-stopped
    ports:
      - "80:3000"
    environment:
      NODE_ENV: production
      PORT: "3000"
      HOSTNAME: "0.0.0.0"
